# Todo App Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a mobile Vue 3 Todo app with add/toggle/edit/delete/filter and localStorage persistence via Pinia.

**Architecture:** Pinia store owns all state and syncs to localStorage via `watch`. Three focused components (`TodoInput`, `TodoFilter`, `TodoList`) read from the store and call its actions. `App.vue` composes them with no business logic.

**Tech Stack:** Vue 3, Vite, Pinia, native CSS, localStorage

> **Note:** No test runner is configured. Each task ends with a browser verification step instead of automated tests.

---

## File Map

| File | Action | Purpose |
|------|--------|---------|
| `src/main.js` | Modify | Register Pinia |
| `src/style.css` | Rewrite | Mobile Todo styles |
| `src/App.vue` | Rewrite | Compose three components |
| `src/stores/todos.js` | Create | Pinia store + localStorage sync |
| `src/components/TodoInput.vue` | Create | Input + add button |
| `src/components/TodoFilter.vue` | Create | Filter tabs |
| `src/components/TodoList.vue` | Create | List with toggle/edit/delete |

---

## Task 1: Install Pinia and register it in main.js

**Files:**
- Modify: `src/main.js`

- [ ] **Step 1: Install Pinia**

Run from root:
```bash
npm install pinia
```
Expected: pinia appears in `package.json` dependencies.

- [ ] **Step 2: Update main.js**

Replace the entire file with:
```js
import { createApp } from 'vue'
import { createPinia } from 'pinia'
import './style.css'
import App from './App.vue'

const app = createApp(App)
app.use(createPinia())
app.mount('#app')
```

- [ ] **Step 3: Verify dev server still starts**

Run: `npm run dev`
Expected: no errors in terminal, page loads in browser.

- [ ] **Step 4: Commit**
```bash
git add src/main.js package.json package-lock.json
git commit -m "feat: install and register Pinia"
```

---

## Task 2: Create the Pinia store

**Files:**
- Create: `src/stores/todos.js`

- [ ] **Step 1: Create the file**

Create `src/stores/todos.js`:
```js
import { defineStore } from 'pinia'
import { ref, computed, watch } from 'vue'

export const useTodosStore = defineStore('todos', () => {
  const todos = ref(JSON.parse(localStorage.getItem('todos') || '[]'))
  const filter = ref('all')

  watch(todos, (val) => {
    localStorage.setItem('todos', JSON.stringify(val))
  }, { deep: true })

  const filteredTodos = computed(() => {
    if (filter.value === 'active') return todos.value.filter(t => !t.done)
    if (filter.value === 'done') return todos.value.filter(t => t.done)
    return todos.value
  })

  function addTodo(text) {
    todos.value.push({ id: crypto.randomUUID(), text, done: false })
  }

  function toggleTodo(id) {
    const todo = todos.value.find(t => t.id === id)
    if (todo) todo.done = !todo.done
  }

  function editTodo(id, text) {
    const todo = todos.value.find(t => t.id === id)
    if (todo) todo.text = text
  }

  function deleteTodo(id) {
    todos.value = todos.value.filter(t => t.id !== id)
  }

  function setFilter(value) {
    filter.value = value
  }

  return { todos, filter, filteredTodos, addTodo, toggleTodo, editTodo, deleteTodo, setFilter }
})
```

- [ ] **Step 2: Commit**
```bash
git add src/stores/todos.js
git commit -m "feat: add Pinia todos store with localStorage sync"
```

---

## Task 3: Replace style.css with mobile Todo styles

**Files:**
- Rewrite: `src/style.css`

- [ ] **Step 1: Replace the entire file**

```css
*, *::before, *::after {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

html, body {
  font-size: 16px;
  -webkit-text-size-adjust: 100%;
}

:root {
  --text: #6b6375;
  --text-h: #08060d;
  --bg: #fff;
  --border: #e5e4e7;
  --accent: #aa3bff;
  --accent-bg: rgba(170, 59, 255, 0.1);
  --accent-border: rgba(170, 59, 255, 0.5);

  font-family: system-ui, 'Segoe UI', Roboto, sans-serif;
  color: var(--text);
  background: var(--bg);
  -webkit-font-smoothing: antialiased;
}

@media (prefers-color-scheme: dark) {
  :root {
    --text: #9ca3af;
    --text-h: #f3f4f6;
    --bg: #16171d;
    --border: #2e303a;
    --accent: #c084fc;
    --accent-bg: rgba(192, 132, 252, 0.15);
    --accent-border: rgba(192, 132, 252, 0.5);
  }
}

/* Layout */
.app {
  max-width: 480px;
  margin: 0 auto;
  padding: 32px 16px;
  min-height: 100svh;
  display: flex;
  flex-direction: column;
  gap: 16px;
}

h1 {
  font-size: 28px;
  font-weight: 600;
  color: var(--text-h);
  text-align: center;
  margin-bottom: 8px;
}

/* TodoInput */
.todo-input {
  display: flex;
  gap: 8px;
}

.todo-input input {
  flex: 1;
  padding: 12px 16px;
  border: 1px solid var(--border);
  border-radius: 8px;
  font-size: 16px;
  background: var(--bg);
  color: var(--text-h);
  outline: none;
}

.todo-input input:focus {
  border-color: var(--accent);
}

.todo-input button {
  padding: 12px 20px;
  background: var(--accent);
  color: #fff;
  border: none;
  border-radius: 8px;
  font-size: 16px;
  cursor: pointer;
  white-space: nowrap;
}

.todo-input button:disabled {
  opacity: 0.4;
  cursor: not-allowed;
}

/* TodoFilter */
.todo-filter {
  display: flex;
  gap: 8px;
}

.todo-filter button {
  flex: 1;
  padding: 8px;
  border: 1px solid var(--border);
  border-radius: 6px;
  background: transparent;
  color: var(--text);
  font-size: 14px;
  cursor: pointer;
}

.todo-filter button.active {
  background: var(--accent-bg);
  border-color: var(--accent-border);
  color: var(--accent);
}

/* TodoList */
.todo-list {
  list-style: none;
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.todo-list .empty {
  text-align: center;
  color: var(--text);
  padding: 40px 0;
  font-size: 14px;
}

.todo-list li {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 14px 12px;
  border: 1px solid var(--border);
  border-radius: 8px;
}

.todo-list li.done span {
  text-decoration: line-through;
  opacity: 0.45;
}

.todo-list li input[type="checkbox"] {
  width: 20px;
  height: 20px;
  accent-color: var(--accent);
  flex-shrink: 0;
  cursor: pointer;
}

.todo-list li span {
  flex: 1;
  font-size: 16px;
  color: var(--text-h);
  word-break: break-all;
  cursor: default;
}

.todo-list li input[type="text"] {
  flex: 1;
  padding: 4px 8px;
  border: 1px solid var(--accent);
  border-radius: 4px;
  font-size: 16px;
  background: var(--bg);
  color: var(--text-h);
  outline: none;
}

.todo-list .delete {
  background: transparent;
  border: none;
  color: var(--text);
  font-size: 22px;
  line-height: 1;
  cursor: pointer;
  padding: 0 2px;
  flex-shrink: 0;
}

.todo-list .delete:hover {
  color: var(--accent);
}
```

- [ ] **Step 2: Commit**
```bash
git add src/style.css
git commit -m "feat: replace style.css with mobile Todo styles"
```

---

## Task 4: Create TodoInput.vue

**Files:**
- Create: `src/components/TodoInput.vue`

- [ ] **Step 1: Create the file**

```vue
<script setup>
import { ref } from 'vue'
import { useTodosStore } from '../stores/todos'

const store = useTodosStore()
const text = ref('')

function add() {
  const trimmed = text.value.trim()
  if (!trimmed) return
  store.addTodo(trimmed)
  text.value = ''
}
</script>

<template>
  <div class="todo-input">
    <input
      v-model="text"
      placeholder="添加新任务..."
      @keyup.enter="add"
    />
    <button :disabled="!text.trim()" @click="add">添加</button>
  </div>
</template>
```

- [ ] **Step 2: Commit**
```bash
git add src/components/TodoInput.vue
git commit -m "feat: add TodoInput component"
```

---

## Task 5: Create TodoFilter.vue

**Files:**
- Create: `src/components/TodoFilter.vue`

- [ ] **Step 1: Create the file**

```vue
<script setup>
import { useTodosStore } from '../stores/todos'

const store = useTodosStore()
const filters = [
  { value: 'all', label: '全部' },
  { value: 'active', label: '未完成' },
  { value: 'done', label: '已完成' },
]
</script>

<template>
  <div class="todo-filter">
    <button
      v-for="f in filters"
      :key="f.value"
      :class="{ active: store.filter === f.value }"
      @click="store.setFilter(f.value)"
    >
      {{ f.label }}
    </button>
  </div>
</template>
```

- [ ] **Step 2: Commit**
```bash
git add src/components/TodoFilter.vue
git commit -m "feat: add TodoFilter component"
```

---

## Task 6: Create TodoList.vue

**Files:**
- Create: `src/components/TodoList.vue`

- [ ] **Step 1: Create the file**

```vue
<script setup>
import { ref } from 'vue'
import { useTodosStore } from '../stores/todos'

const store = useTodosStore()
const editingId = ref(null)
const editingText = ref('')

function startEdit(todo) {
  editingId.value = todo.id
  editingText.value = todo.text
}

function commitEdit(id) {
  const trimmed = editingText.value.trim()
  if (trimmed) store.editTodo(id, trimmed)
  editingId.value = null
}

function cancelEdit() {
  editingId.value = null
}
</script>

<template>
  <ul class="todo-list">
    <li v-if="store.filteredTodos.length === 0" class="empty">
      暂无任务
    </li>
    <li
      v-for="todo in store.filteredTodos"
      :key="todo.id"
      :class="{ done: todo.done }"
    >
      <input
        type="checkbox"
        :checked="todo.done"
        @change="store.toggleTodo(todo.id)"
      />
      <span v-if="editingId !== todo.id" @dblclick="startEdit(todo)">
        {{ todo.text }}
      </span>
      <input
        v-else
        v-model="editingText"
        type="text"
        @keyup.enter="commitEdit(todo.id)"
        @keyup.escape="cancelEdit"
        @blur="commitEdit(todo.id)"
      />
      <button class="delete" @click="store.deleteTodo(todo.id)">×</button>
    </li>
  </ul>
</template>
```

- [ ] **Step 2: Commit**
```bash
git add src/components/TodoList.vue
git commit -m "feat: add TodoList component with toggle, edit, delete"
```

---

## Task 7: Update App.vue and verify the full app

**Files:**
- Rewrite: `src/App.vue`

- [ ] **Step 1: Rewrite App.vue**

```vue
<script setup>
import TodoInput from './components/TodoInput.vue'
import TodoFilter from './components/TodoFilter.vue'
import TodoList from './components/TodoList.vue'
</script>

<template>
  <div class="app">
    <h1>Todo</h1>
    <TodoInput />
    <TodoFilter />
    <TodoList />
  </div>
</template>
```

- [ ] **Step 2: Open browser and verify each feature**

Run `npm run dev` and check:
1. 页面加载，空列表显示"暂无任务"
2. 输入任务按回车 → 条目出现
3. 输入任务点击添加按钮 → 条目出现
4. 输入为空时添加按钮禁用
5. 点击复选框 → 文字加删除线，条目进入已完成状态
6. 切换到"未完成"筛选 → 已完成条目隐藏；"已完成"筛选 → 只显示已完成
7. 双击条目文字 → 变为可编辑输入框，按回车保存
8. 编辑时按 Escape → 取消编辑，恢复原文字
9. 点击 × → 条目删除
10. 刷新页面 → 数据仍在（localStorage 生效）

- [ ] **Step 3: 手机验证（可选）**

用手机浏览器打开终端显示的 `Network:` 地址，验证触摸交互正常。

- [ ] **Step 4: Commit**
```bash
git add src/App.vue
git commit -m "feat: compose Todo app in App.vue"
```
